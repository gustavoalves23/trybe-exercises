const capsString = (string) => string.toUpperCase();
const firstLetter = (string) => string[0];
const joinStrings = (string1,string2) => `${string1} ${string2}`

module.exports = {
  capsString,
  firstLetter,
  joinStrings,
}